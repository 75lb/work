var AsyncJob = require("../lib/AsyncJob"),
    assert = require("assert"),
    l = console.log;
    
describe("in-command", function(){
    it("should have access to the current job");
});

it("declare job commands and execution plan separately");
/*
work.registerJob("encodeSOmething", function(file, options){
    // etc
});
work.registerJob("removeSomething", function(file, options){
    // etc
});
work.executionPlan([
{ job: "encodeSomething", args: ["film.api", { preset: Normal }], }
])

*/